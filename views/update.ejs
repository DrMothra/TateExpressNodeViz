<!DOCTYPE html>
<html>
<head>
    <title>Tate Node Visualiser</title>

    <script type="text/javascript" src="js/jquery-1.11.2.min.js"></script>
    <script type="text/javascript" src="js/bootstrap.min.js"></script>
    <script type="text/javascript" src="js/bootstrap3-typeahead.min.js"></script>
    <script type="text/javascript" src="js/updateNode.js"></script>

    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/bootstrap-theme.min.css">
    <link rel='stylesheet' href="css/tateExpressStyles.css">

</head>
<body>
    <h1>Tate Node Visualiser</h1>

    <div id="instructions" class="infoPanel">
        <a href="tateInstructionsUpdate.html" target="_blank"><img src="images/info.png"></a>
    </div>

    <div id="updateNode" class="title">
        <h2>Update Node</h2>
        <h3>Graph - <span id="graphName"><%= graphName %></span></h3>
        <form id="searchForm" class="form-horizontal" action="/searchGraph" method = "post" autocomplete="off">
            <div class="form-group">
                <label for="graphID" class="col-md-1 control-label">Graph ID</label>
                <div class="col-md-5">
                    <input type="text" class="form-control" name="graphID" id="graphID" value="<%= graphID %>">
                </div>
            </div>
            <div class="form-group">
                <label for="node_Name" class="col-md-1 control-label">Node Name</label>
                <div class="col-md-5">
                    <input type="text" class="form-control" name="nodeValue" id="node_Name" value="<%= node_Name %>">
                </div>
            </div>
            <button id="search" type="submit" class="btn btn-primary">Get Links</button>
        </form>
    </div>

    <div id="links" class="title">
        <h2>Links</h2>
        <div class="container-fluid">
        <% for(var j=0; j<nodes.length; ++j) { %>
        <h3><%= nodes[j] %></h3>

            <% var links = linkData.length === 0 ? [] : linkData[j]; %>
            <% for(var i=0; i<links.length; ++i) { %>
            <div class="row">
                <div class="col-md-3">
                    <ul>
                        <li><%= links[i].linkType %> <%= links[i].linkTo %></li>
                    </ul>
                </div>
                <div class="col-md-2">
                    <button class="btn btn-primary" id='<%="yesLink" + i %>' type="button">Agree</button><button class="btn btn-primary" id='<%="noLink" + i %>' type="button">Disagree</button>
                </div>
                <div class="col-md-2">
                    <span id='<%="updated" + i %>' class="label label-success noDisplay">Updated</span>
                </div>
                <div class="col-md-5">
                    &nbsp
                </div>
            </div>
            <% } %>
        <% } %>
        </div>
    </div>

    <div class="title">
        <button id="backToModify" type="submit" class="btn btn-primary">Modify Graph</button>
    </div>
    <br>
    <form method="get" action="/showGraphs">
        <div class="form-group title">
            <button id="back" type="submit" class="btn btn-primary">Home</button>
        </div>
    </form>
</body>
</html>
